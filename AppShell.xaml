<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:CopilotCostTracker"
       xmlns:views="clr-namespace:CopilotCostTracker.Views"
       xmlns:viewmodels="clr-namespace:CopilotCostTracker.ViewModels"
       x:Class="CopilotCostTracker.AppShell"
       x:DataType="viewmodels:MainViewModel"
       Shell.NavBarIsVisible="False"
       FlyoutBehavior="Flyout"
       FlyoutWidth="220"
       FlyoutBackgroundColor="{StaticResource SurfaceSecondaryColor}">

    <!-- Nav item template: icon + label -->
    <Shell.ItemTemplate>
        <DataTemplate>
            <Grid HeightRequest="44" Padding="0">
                <VisualStateManager.VisualStateGroups>
                    <VisualStateGroupList>
                        <VisualStateGroup x:Name="CommonStates">
                            <VisualState x:Name="Normal">
                                <VisualState.Setters>
                                    <Setter Property="BackgroundColor" Value="Transparent" />
                                </VisualState.Setters>
                            </VisualState>
                            <VisualState x:Name="Selected">
                                <VisualState.Setters>
                                    <Setter Property="BackgroundColor" Value="{StaticResource BorderColor}" />
                                </VisualState.Setters>
                            </VisualState>
                        </VisualStateGroup>
                    </VisualStateGroupList>
                </VisualStateManager.VisualStateGroups>
                <HorizontalStackLayout Spacing="12" Padding="14,0" VerticalOptions="Center">
                    <Image Source="{Binding Icon}"
                           WidthRequest="18" HeightRequest="18"
                           VerticalOptions="Center"
                           Opacity="0.85" />
                    <Label Text="{Binding Title}"
                           TextColor="{StaticResource TextPrimaryColor}"
                           FontSize="14"
                           VerticalOptions="Center" />
                </HorizontalStackLayout>
            </Grid>
        </DataTemplate>
    </Shell.ItemTemplate>

    <!-- ─── Flyout header ─────────────────────── -->
    <Shell.FlyoutHeader>
        <VerticalStackLayout Padding="16,20,16,8" Spacing="2">
            <Label Text="Copilot"
                   FontSize="18" FontAttributes="Bold"
                   TextColor="{StaticResource AccentBlueColor}" />
            <Label Text="Cost Tracker"
                   FontSize="11"
                   TextColor="{StaticResource TextSecondaryColor}" />
        </VerticalStackLayout>
    </Shell.FlyoutHeader>

    <!-- ─── Pages ────────────────────────────── -->
    <FlyoutItem Title="Overview" Icon="nav_overview.png" Route="main">
        <ShellContent ContentTemplate="{DataTemplate local:MainPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Sessions" Icon="nav_sessions.png" Route="sessions">
        <ShellContent ContentTemplate="{DataTemplate views:SessionsPage}" />
    </FlyoutItem>

    <FlyoutItem Title="By model" Icon="nav_bymodel.png" Route="bymodel">
        <ShellContent ContentTemplate="{DataTemplate views:ByModelPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Trend" Icon="nav_trend.png" Route="trend">
        <ShellContent ContentTemplate="{DataTemplate views:TrendPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Settings" Icon="nav_settings.png" Route="settings">
        <ShellContent ContentTemplate="{DataTemplate views:SettingsPage}" />
    </FlyoutItem>

    <!-- ─── Flyout footer ─────────────────────── -->
    <Shell.FlyoutFooter>
        <VerticalStackLayout Padding="12,8" Spacing="4">
            <BoxView HeightRequest="0.5" Color="{StaticResource BorderColor}" Margin="0,0,0,4" />
            <Button Text="＋ Add folder"
                    Command="{Binding GoToFoldersCommand}"
                    Style="{StaticResource GhostButtonStyle}"
                    HorizontalOptions="Fill"
                    FontSize="13"
                    HeightRequest="36" />
            <Label Text="{Binding LastUpdated}"
                   FontSize="10"
                   Style="{StaticResource TextTertiaryStyle}"
                   Margin="4,2,0,0" />
        </VerticalStackLayout>
    </Shell.FlyoutFooter>

</Shell>
